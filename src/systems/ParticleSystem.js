/**
 * Manages floating petal and dust mote particles.
 * Sprint 1: empty stub.
 * Sprint 4+: GPU-based BufferGeometry particle systems.
 */
export class ParticleSystem {
  /** @param {import('./SceneManager').SceneManager} sceneManager */
  constructor(sceneManager) {
    this.sceneManager = sceneManager;
  }

  /** Instantiates particle geometry and adds to scene. */
  init() {
    // Populated in Sprint 4
  }

  /** @param {number} elapsed - total seconds since start */
  update(_elapsed) {
    // Populated in Sprint 4
  }
}
